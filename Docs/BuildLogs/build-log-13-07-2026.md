## Session 1: 13-07-2026
### Duration: 2 Hours
### Tasks:
    - Created a comprehensive Google Test (GTest) test suite for the `Vector` class.
    - Replaced the old demo/fake `VectorInsertTest.cpp` (which used `DynamicArray`) with a proper test file using `Vector<T>`.
    - Created 9 test files in `tests/Vector/` covering all public methods of the `Vector` class:
        - `VectorConstructorTest.cpp` — Default constructor, initializer_list constructor, copy constructor, copy assignment operator (21 tests).
        - `VectorPushBackTest.cpp` — `push_back` with single/multiple elements, resize triggers, large dataset (8 tests).
        - `VectorPopBackTest.cpp` — `pop_back` with empty vector exception, pop all elements (9 tests).
        - `VectorInsertTest.cpp` — `insert` at beginning/middle/end, empty vector, out-of-range (11 tests).
        - `VectorRemoveTest.cpp` — `remove` at all positions, shift verification, out-of-range (10 tests).
        - `VectorClearTest.cpp` — `clear` preserves capacity, reuse after clear (7 tests).
        - `VectorAccessTest.cpp` — `operator[]`, `at()`, `front()`, `back()` both const and non-const (24 tests).
        - `VectorCapacityTest.cpp` — `size()`, `capacity()`, `empty()`, `reserve()`, `shrink_to_fit()` (27 tests).
        - `VectorSearchTest.cpp` — `find()`, `contains()` with duplicates, empty vector, string type (18 tests).
    - Installed `mingw-w64-ucrt-x86_64-gtest` (v1.17.0) via MSYS2 pacman.
    - Compiled and ran all 135 tests — all passed.

### Problem Encountered:
    - GTest was not installed on the system. Resolved by installing `mingw-w64-ucrt-x86_64-gtest` via MSYS2 pacman.
    - The initial pacman download had a connection reset error, but the package still installed successfully.
