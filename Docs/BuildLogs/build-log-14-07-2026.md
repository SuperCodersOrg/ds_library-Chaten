## Session 2: 14-07-2026
### Duration: 2.5 Hours
### Tasks:
    - Created a comprehensive Google Test (GTest) test suite for the `List` class.
    - Implemented linked list test one.
    - Created 6 test files in `tests/LinkedList/` covering all public methods of the `List` class:
        - `ListConstructorTest.cpp` — Default constructor, initializer_list constructor, copy constructor, copy assignment operator (19 tests).
        - `ListPushPopTest.cpp` — `push_back` and `push_front` with single/multiple elements/large dataset, `pop_back` and `pop_front` with safety checks (23 tests).
        - `ListInsertRemoveTest.cpp` — `insert` and `remove` at beginning/middle/end, out-of-range checks (17 tests).
        - `ListAccessTest.cpp` — `front()`, `back()`, `at()`, `empty()`, `size()`, `clear()` (24 tests).
        - `ListSearchTest.cpp` — `find()` returning node pointers, `contains()` check (17 tests).
        - `ListReverseTest.cpp` — `reverse()` with empty, single, and multiple elements (5 tests).
    - Compiled and ran all 105 tests — all passed.
